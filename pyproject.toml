[tool.black]
line-length = 100
target-version = ["py313"]

[tool.isort]
line_length = 100
profile = "black"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
fixable = ["ALL"]

[tool.ruff.lint.per-file-ignores]
"migrations/env.py" = ["E402"]
